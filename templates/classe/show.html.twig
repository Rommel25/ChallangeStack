{% extends 'base.html.twig' %}

{% block title %}Classe{% endblock %}

{% block body %}
     {% if eleves|length > 0 %}
        <table class="w-full text-sm text-left rtl:text-right text-gray-500 dark:text-gray-400">
            <caption class="p-5 text-lg font-semibold text-left rtl:text-right text-gray-900 bg-white dark:text-white dark:bg-gray-800">
                Liste des élèves dans la classe : {{ classe.section }}<p class="mt-1 text-sm font-normal text-gray-500 dark:text-gray-400">Parcourez la liste des élèves que vous avez dans cette classe</p>
            </caption>
            <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
            <tr>
                <th scope="col" class="px-6 py-3">Nom</th>
                <th scope="col" class="px-6 py-3">Prénom</th>
                <th scope="col" class="px-6 py-3">Voir l'élève</th>
                <th scope="col" class="px-6 py-3">Modifier l'élève</th>
            </tr>
            </thead>
            <tbody>
            {% for eleve in eleves %}
                <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700">
                    <td class="px-6 py-4">{{ eleve.user.nom }}</td>
                    <td class="px-6 py-4">{{ eleve.user.prenom }}</td>
                    <td class="px-6 py-4">
                        <a href="{{ path('app_eleve_show', { id: eleve.id }) }}" class="font-medium text-blue-600 dark:text-blue-500 hover:underline">Voir élève</a>
                    </td>
                    <td class="px-6 py-4">
                        <a href="{{ path('app_eleve_edit', { id: eleve.id }) }}" class="font-medium text-blue-600 dark:text-blue-500 hover:underline">Modifier élève</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% else %}
    {% endif %}
    <div class="flex justify-center items-center mt-4">
        <a href="/eleve/new">
            <button type="button" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800">
                Ajouter un élève
            </button>
        </a>
    </div>
{% endblock %}
